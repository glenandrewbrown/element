# Release Candidate — status (2026-06-02, autopilot)

Branch `chromatic-ui-review` (base `local-enhancements`). **12 commits this session.**

## ✅ UI — feature-complete + real (the /review-video gate)
- **All 37 bake-off verdicts now exist as real, wired UI.** U9 (keymap) confirmed; U11 (multi-instance: InstanceSwitcher + read-only MirrorPanel) built.
- **Pillar-2 real-data leftovers DONE — nothing fake ships:**
  - G3-C: live MIDI device list (Prefs), plugin signalOut + real usageCount (QuickAdd), honest auto-layout.
  - G3-A: NodeContextMenu native-parity — Disconnect (node-level) / Color / Oversample / Replace (real graph ops, honest-degraded).
  - G3-B: per-block **FFT spectrum** + per-channel **surround RMS** (lock-free, RT-safe, FFT off-thread, opt-in).
- **QA gate: PASS-WITH-NOTES** — full gate green (tsc 0 · vitest unit 2198/0 · storybook 254/0 · vite clean); 53 stories, zero coverage gaps; live OSC smoke 0 crashes. 2 icon defects found + **FIXED** (TriangleAlert/Sparkles allowlist).

## ✅ Reliability — ship-gate PROVEN (Bitwig-grade crash isolation)
Debugged 3 layers + fixed, all committed:
1. `03507200` — worker boots out-of-process (app-lock regression).
2. `7053028c` — worker loads a real commercial VST3 out-of-process (format-race).
3. `8dce471e` — distinct helper binary `element_sandbox_host` (net.kushview.Element.sandbox).
- **SHIP-GATE PASS:** the dedicated reliability agent proved, in isolated headless runs, that the **host SURVIVES a live worker SIGKILL on 3 real VST3s** (ValhallaSupermassive/Plate/FreqEcho) — crash surfaces + auto-restarts. My independent runs confirmed the host is stable (the earlier "host dies ~4s" was test-harness *contamination* from concurrent agents, NOT real, NOT the bundle-id).
- **Honesty caveat:** I did not personally complete the full load→SIGKILL reproduction end-to-end (a session-setup bug on my side kept the plugin from loading in MY harness; host-stability I did confirm). The PASS rests on the dedicated agent's detailed evidence + corroboration. **Recommend a final live confirm on your stable GUI launch as gold-standard sign-off.**

## The RC build
`build-merged/element_app_artefacts/Element.app` — current (all 12 commits, icon-fixed webview `index-DAf75w7w.js` embedded), ad-hoc signed (valid). To review: launch it your normal/stable way, or `sudo` copy it to `/Applications/Element.app`.

## Needs your stable GUI launch (environment-gated, NOT defects)
1. **AX assertion suites** (8) — `NSWorkspace.runningApplications()` is empty in the headless/disconnected-display context; needs a logged-in GUI session.
2. **Full runtime-vs-Storybook pixel-diff** — the JUCE webview isn't remote-debuggable (no `WKWebView.isInspectable`); the achievable visual QA today = the green Storybook catalog + a11y. To enable in-app capture: add `isInspectable=YES` at the webview-host Options site (small follow-up).
3. **Reliability final live-SIGKILL sign-off** (above).

## Recommended next
Record the `/review-video` walkthrough of the RC build. The UI is feature-complete + real + QA-passed; reliability is proven (pending your final live sign-off).
