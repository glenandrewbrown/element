# HANDOFF — 2026-06-02 ultrawork session (crash-isolation + Pillar-2 + UI)

Branch `chromatic-ui-review`. **TRUST GIT + fresh verification over any claim here.**

## ✅ SHIPPED + VERIFIED (committed)
1. **`5078290c` — crash-isolation host-survival fix (THE #1 ship blocker — SOLVED).**
   Root cause (confirmed by 2 opus tracers + a 3-case natural experiment): the sandbox worker re-execs the same `Element` Mach-O under the SAME `CFBundleIdentifier net.kushview.Element` and registered as a *foreground* (Regular) NSApplication → macOS duplicate-instance enforcement SIGKILLed the host; compounded by a synchronous re-entrant restart on the IPC thread (self-join/hang) + the worker sharing the host's process group (no setsid).
   Fix: worker `NSApplicationActivationPolicyProhibited` (Accessory, not Regular, for the editor) + `setsid()` + async/off-IPC-thread/bounded restart.
   **VERIFIED — host ISOLATION (the catastrophic part) is SOLID:** headless `SandboxRealPluginCrashTest` (real ValhallaSupermassive VST3 hosted out-of-process → `kill -9` worker → host survives + crash detected), `SandboxRealProcessTests`, `SandboxIsolationTests`, `SandboxAtomicRef` all GREEN. **LIVE-PROVEN:** launched the signed build under `pluginSandboxMode=1` autoloading the pizmidi `force-2-range` session (the EXACT scenario that crashed the host on launch pre-fix) → **host survived 70s+** (probe log: `setsid() ok — worker in own session/pgrp`; workers spawn; no SIGKILL; no `.ips`). The catastrophic launch-crash / host-SIGKILL is GONE.
   **⚠️ NOT fully solid — auto-RECOVERY is FLAKY (known, pre-existing in 5078290c, root-caused below).** `SandboxCrashOnLoadTest` is **flaky (≈1/6→5/8 solo)** — an earlier "green" was a lucky pass. The flakiness is NOT in isolation (host always survives) but in the RELAUNCH+RELOAD recovery: see #11/#13. So "a bad plugin can't kill my session" (isolation) holds; "and it auto-recovers the plugin" does not yet hold reliably.
2. **`3824c836` — D4 real per-plugin CPU.** Lock-free render-time tap (EMA, 1 relaxed atomic, RT-safe, mirrors `setOutputRMS`) → `cpuLoad%` on the message thread → Block CPU pill (already wired). `RenderNanosTests` green. Replaces the hardcoded `cpuLoad=0.0` FIXME.
3. **`30c6ab91` — U9 (webview half):** `nativeOpenKeymapEditor()` bridge stub (msg `elementOpenKeymapEditor`) + "Edit key commands…" button in Prefs→Shortcuts. + fixed a latent `tsc -b` drift in `PreferencesModal.gaps.test.tsx`. 4/4 tests, webview tsc-clean.
- **D5 spectrum:** already honest ("No spectrum" placeholder, fake bezier already removed) — closed, no work needed.
- **Plugins-panel search (#7):** root cause = stale webview `dist/` bundled in the app; rebuilt webview (tsc 0) → D4's element_app relink re-bundled fresh dist. (Live search re-verify still advisable.)

## ⏳ FOLLOW-UPS (open)
- **#11/#13 crash-iso HARDENING + the RECOVERY-RACE — patch at `.omo/patches/crashiso-hardening-WIP-2026-06-02.patch` (773 lines), reverted from tree.** The patch implements: atomic `restartAttempts`, spawn-failure budget (no infinite loop), Idle early-return, Accessory-before-setVisible, RT `prepareToPlay` reconfiguring-guard, + `RepeatedLoadCrash`/`LoadsOkThenLaterCrash`/`PersistentSpawnFailure` tests + a `protected virtual launchWorkerProcess()` test seam. Reverted because the new test isn't deterministically green YET — because of a **REAL, PRE-EXISTING recovery race in 5078290c** that a96fb2 root-caused (THIS is the #1 thing to fix to make crash-recovery reliable):
  > **Recovery-load race (sandboxhost.hpp `attemptRestart`):** after `launchWorkerProcess()` returns, the code sets `state=Ready` and calls `loadPlugin()` **synchronously**. JUCE's `launchWorkerProcess()` returns once the child is *spawned*, **before the IPC pipe handshake completes** → the immediate `LoadPlugin` write **races the connection and is frequently dropped** → the relaunched worker boots, idles at `Ready`, heartbeats forever, never re-attempts the crashing load → host never observes the re-crash → never reaches give-up `Error` → 14s stall. Measured flaky **1/6 → 5/8 solo**.
  > **FIX (a96fb2, partial → 5/8; finish it):** DEFER the recovery `loadPlugin` until the relaunched worker confirms the pipe is up — gate on a connect-ack / `Prepared` (first heartbeat alone was insufficient for the residual race). ALSO: the `handleConnectionLost` early-return-on-`State::Crashed` guard (added in 5078290c to stop a Crashed↔Error spin) can **silently drop a genuine worker crash that lands during the relaunch handshake window** — suppress only OUR OWN `killWorkerProcess` teardown via an explicit flag instead of gating on `Crashed`.
  This is the residual ship-gate for crash *recovery* (isolation itself is solid). The "exit 1 ~90s" I saw in the live pizmidi run is consistent with this race under sustained churn. Re-apply the patch + finish the connect-ack gating, then re-run `SandboxCrashOnLoadTests` SOLO until deterministically green (≥10×).
- **#6 U9 C++ bridge:** add `withNativeFunction("elementOpenKeymapEditor", …)` in `element_webview_host.cpp` (pattern at :943, siblings `elementOpenLuaConsole`:2315 / `elementOpenGraphMixer`:2323) → open native `KeymapEditorView` (src/ui/keymapeditorview.cpp) as a `DialogWindow` (convert `guiservice.cpp:841 Commands::showKeymapEditor` from content-view-swap).
- **#12 H1 DURABLE fix (per reviewer):** activation-policy is a behavioural mitigation, NOT structurally certain. Ship the worker as a separate helper bundle w/ its own `CFBundleIdentifier` (Chrome-Helper style) so duplicate-instance enforcement is inapplicable by construction. CMake: new helper target (~CMakeLists.txt:72 BUNDLE_ID); point `SandboxHost::launchWorkerProcess` (sandboxhost.hpp:~695) at it. Gated on whether the activation-policy live-proof holds (it did for launch-survival).
- **#8 U8:** native `preferences.cpp` dialog still wired in `mainmenu.cpp` alongside the webview `PreferencesModal` → route menu to the webview overlay / remove legacy.
- **#9 R2:** real-AU load-time message-thread hang (`settings.cpp:583`, BRASS_4Horns) → blocks R6 AU-first default-ON. Use the diagnose loop.
- **#10 U11 multi-instance:** AMBIGUOUS — needs Glen's call: does "instance" = graph tabs / project windows / OS processes? Scope agent: L. Webview InstanceSwitcher + MirrorPanel + native registry.

## ⚠️ ENV / GOTCHAS
- **graphify watcher + commit-hook spike Ollama to ~600%×2 (load → 221 on 32 cores)** on every commit/file-change → thrashes builds + the whole machine. I `pkill -9 -f graphify` after each commit. Consider disabling the graphify hook for heavy build sessions.
- conf restored: `pluginSandboxMode=0`, `defaultNewSessionFile=…/Sessions/Default.els`. Backup at `/tmp/Element.conf.bak-*`.
- `tools/reliability/sign-dev-build.sh` re-signs element_app (relinks strip the ad-hoc sig); needed before any real-plugin sandbox run. Live proof recipe: set mode=1 + `defaultNewSessionFile` to `~/Music/Element/Graphs/midi-force-2-range-clarinet.elg`, launch detached, check host pid survives.
- Per-commit gate MUST include `vitest --project unit` AND `tsc -b` (story-tests alone missed a tsc drift).
- Stray untracked: `test/PluginManagerEdgeCaseTests.cpp` (corrupted-204f5545 artifact, runtime-disabled), `webview/src/components/canvas/__tests__/QuickAddPopup.search.test.tsx` — triage in a fresh session.
- build-merged element_app binary is currently the +126 (reverted-source) build — rebuild from HEAD for a clean binary.
