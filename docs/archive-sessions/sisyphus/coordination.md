# Element Wave 1 — Cross-Team Coordination Log

**Wave start SHA:** `4105eb23` (`local-enhancements`)
**Baseline:** 48/48 ctest green, build-merged green, webview/dist fresh
**Glen-ratified scope (2026-05-07):** Phase D IN (Wave 2), Phase G OUT, autonomous commits ON, 4 gates

## Team file scopes (no overlap)

| Team | Owns | Forbidden |
|---|---|---|
| **B** (crash + RT) | `src/engine/`, `src/nodes/` (except `scriptnode.cpp`), `src/services/`, `test/engine/`, `test/integration/` | `webview/`, `src/scripting/`, `src/lua/`, `src/nodes/scriptnode.cpp`, `src/nodes/sandbox*.hpp` |
| **E** (Lua security) | `src/scripting/`, `src/lua/` (bindings only — not the build patches), `src/nodes/scriptnode.cpp`, `test/scripting/` | `webview/`, `src/engine/`, `src/services/`, `src/nodes/*` (except scriptnode) |
| **U** (UI + tests) | `webview/`, `test/services/` | `src/`, `cmake/`, anything outside the two above |

**Conflict rule:** if a fix appears to require a file outside your scope, escalate via this log rather than editing — the other team will land it.

**Sandbox files (`src/engine/sandbox*.hpp`, `src/nodes/sandboxedprocessor.hpp`) are RESERVED for Wave 2 / Team S — DO NOT TOUCH in Wave 1.**

## Cross-team facts (append-only)

<!-- Each team appends a single line per published fact. Format: `YYYY-MM-DD HH:MM | Team X | <fact> | HEAD <SHA>` -->
2026-05-07 20:50 | Team B | All 7 Phase B crash fixes (B-1..B-10 except B-3 which is implicit) verified already-applied at Wave 1 start; only net work was test/engine/RootGraphMissingNodeTest.cpp (B-1 regression cover, 4 cases) — services no longer null-deref via sibling<GuiService>; ctest 48 → 49 | HEAD 271fa8a3
2026-05-07 20:50 | Team B | Plan §3.2 F-1 mislocates bug — symbol is `GraphNode::connectChannels` in src/engine/graphnode.cpp:239, NOT graphbuilder.cpp | HEAD 271fa8a3
2026-05-07 21:12 | Team U | <Icon name size className tone /> component published at webview/src/components/neu/Icon.tsx — wraps lucide-react@1.14.0, decorative-by-default a11y, 5 tone variants (audio/midi/cv/primary/secondary). Re-exported from webview/src/components/neu/index.ts. Ready for batch 2 + Phase F | HEAD 24787af3
2026-05-07 21:12 | Team U | F.0.5/F.0.6/F.0.7/F.0.9 batch 1 closed. F.0.7 codemod replaced ICON_* path-strings in 13 files (Toolbar, ToolPalette, StatusBar, MacroDashboard, QuickAccess, SnippetShelf, ConnectionEditor, SceneLauncher, BlockTabStrip, DashboardBuilder, LiveHealth, NodeContextMenu, EdgeContextMenu). All 45 lucide names verified — zero audio-domain icon gaps in this batch. F.0.8 custom-glyph generation deferred until port-shape work needs them | HEAD 00e72e6f
2026-05-07 21:12 | Team U | Vitest now wired at webview/vitest.config.ts (jsdom + @vitejs/plugin-react). `npm run test` runs Icon snapshot suite (7/7, 5 snapshots) | HEAD 24787af3
2026-05-07 21:30 | Team E | Phase E Lua sandbox closeout — E-1 was already applied at Wave 1 start (commits 46c9ae01 + fe576d9b); landed E-3 (writeFile data-path clamp), E-5 (AudioBuffer/MidiBuffer luaL_argcheck bounds), E-6 (DSPScript::validate compile-check), E-7 (lua_sethook 100k-instruction watchdog). Three new test suites green (SandboxIsolationTests, LuaBoundsTests, NodeWriteFileTests) + 3 added DSPScriptTest cases. Full ctest 48 → 51 passing (+3) | HEAD b9309894
2026-05-07 21:30 | Team E | Lua sandbox banned-API set is now closed: io, os, debug, ffi, dofile, loadfile, load, package.loadlib, native package.cpath. Allow-list documented in src/scripting/bindings.cpp comment block above initializeState. LuaSandboxTests + SandboxIsolationTests + LuaBoundsTests + NodeWriteFileTests + DSPScriptTest collectively gate the boundary | HEAD b9309894
2026-05-07 21:55 | Team C | Phase C closeout. C-1/C-2/C-3/C-4/C-6 verified already-applied at Wave 1 start (Phase E refactor of ScriptNode + earlier batches replaced ScopedLock lockMidiOut with getAtomicMidiOutput; AudioRouter+AudioMixer pre-allocate temp buffers in prepareTo*; ProcessBufferOp uses tryEnter; GraphNode render uses avoidReallocating=true). Net new: C-7 (EQFilter direct dispatch instead of std::function lambda) + C-8 (WetDry DBG removal) + Phase C deliverable AudioThreadAllocationTest harness (operator-new override + thread-local guard, smoke 4 cases default ctest, opt-in stress via ELEMENT_RT_STRESS_TEST). C-5 OUT OF SCOPE (Wave 2). Full ctest 51 → 53 passing (+2). | HEAD 61be9285
2026-05-07 21:55 | Team C | audio thread now provably alloc-free in steady state — gated by AudioThreadAllocationTests in default ctest. Any future regression that drops avoidReallocating, prepareTo* pre-allocation, or adds DBG/std::function on a hot path will fire the test. | HEAD 61be9285
2026-05-07 22:05 | Team C | Phase C test harness extended to cover real Element nodes (AudioRouterNode render, EQFilter shape change + processBlock, WetDryProcessor processBlock). 7 cases total in AudioThreadAllocationTests, all green. The harness now confirms the C-3/C-7/C-8 fixed code paths specifically — not just the harness mechanism. | HEAD b2fd81fe
2026-05-07 22:15 | Team F-web | F-10 / Q1 closeout: Icon.tsx refactored to explicit static allowlist (named imports for the 32 icons in use + HelpCircle fallback) and vite.config.ts chunk-splits @xyflow/react / framer-motion / @lottiefiles / react+react-dom. Bundle 1,293 kB → 198.67 kB main (-84%) / 355.59 kB → 49.84 kB gzip (-86%); total JS 1,293 → 687 kB (-47%). Public <Icon name=... /> API unchanged, all 5 prior snapshots still pass. Q1 CLOSED. | HEAD 20ebf871
2026-05-07 22:18 | Team F-web | W-1 / NeuPromptModal: webview/src/components/layout/NeuPromptModal.tsx (controlled component) replaces both window.prompt() call sites in InspectorHub PRESETS Save/Load. role=dialog + aria-modal + aria-labelledby + focus trap (input ↔ Cancel ↔ Confirm) + ESC dismisses + Enter on input confirms with trimmed value. Backdrop click is intentionally NOT a dismiss. 8 new vitest cases + 1 snapshot. Zero remaining window.prompt() in source. | HEAD 15bb5d31
2026-05-07 22:23 | Team F-web | F-9 / About modal upgrade: AboutModal.tsx now drives a four-state machine (idle/checking/requested/error) around elementAppCheckForUpdates instead of fire-and-forget. Surfaces honest copy that Element handles the actual updater check in a separate native window (matches GuiService::checkUpdates contract). About button added to Edit-mode toolbar (was Perform-only). 8 new vitest cases + 3 snapshots (idle/checking/requested). Build number NOT shown — AboutInfo lacks the field; would need a C++ change in element_webview_host.cpp (Team F-cpp scope). | HEAD 4b8619f6
2026-05-07 22:24 | Team F-web | Phase F-block 1 + F-block 3 batch 3 closeout: 3/3 tasks shipped (W-1, F-9, F-10) over 3 atomic commits. webview vitest 18 → 34 passing (+16). webview main bundle 1,293 → 203 kB / 50.99 kB gzip. tsc 0 errors, vitest 100% pass. ctest baseline (53/53) untouched — webview-only diff. | HEAD 4b8619f6
2026-05-07 22:25 | Team F-cpp follow-up | Recommendation: extend AboutInfo / elementAppGetAbout in src/ui/element_webview_host.cpp:2252-2259 to include a `build` property (read from build_number.txt or ELEMENT_BUILD_NUMBER if defined). The webview AboutModal will surface it automatically once the bridge supplies it — see TODO comment in webview/src/components/layout/AboutModal.tsx. Non-blocking for this batch. | HEAD 4b8619f6
2026-05-07 23:32 | Team D | Glen's P0 smoke-test "play/pause/stop/power dead, plugin browser empty, right-click does nothing" diagnosed as 4 separate root causes, all webview-side. D-3 wrong React Flow prop name (`onContextMenu` vs `onPaneContextMenu`) — pane right-click never fired. D-2a Stop/Rewind/Power buttons literally had no onClick handlers (visual placeholders). D-2b Play button toggled engine but had no Zustand mirror of engine.isPlaying — UI never reflected mode. D-1 PluginManager scan is async; single mount-time refresh() ran before scan finished. D-2c (advisor catch, post-Glen): Undo/Redo same dead-button class — wired before regression surfaced. D-4 codemod consumer regression NOT a bug (verified via grep). 5 commits 137c73d4..b106a3ed, +85/-14 lines, vitest 34/34, ctest 67/67, main bundle 203 → 204.24 kB / 51.21 kB gzip. C++ side untouched — all wiring already correct from Wave 1. | HEAD b106a3ed

## Open questions for Gate 1 review

<!-- Each team appends questions Glen needs to answer at Gate 1. Format: `Q[id]: <question> — raised by Team X` -->
Q1: F.0.6 Icon component uses dynamic name resolution (`import * as LucideIcons` + lookup-by-string) which defeats lucide-react tree-shaking — main bundle grew 687kB → 1.29MB (+88%, gzip 206kB → 356kB). Trade-off: keep the ergonomic `<Icon name="..." />` API and accept the bundle, OR refactor Icon.tsx to an explicit allowlist that imports only the ~50 icons currently in use. Allowlist would restore tree-shaking but means every new icon needs an Icon.tsx edit. — raised by Team U — **CLOSED 2026-05-07 22:15 by Team F-web at HEAD 20ebf871** (Option A taken: static named-import allowlist for the 32 icons in use; HelpCircle preserved as the unknown-name fallback; public API unchanged so all 5 prior snapshots pass; main bundle 1,293 → 198 kB (-84%); total JS 1,293 → 687 kB (-47%); the lucide-react@1.14.0 install ships only the named imports we declare).
Q-E-4: read-only Context facade for Lua not attempted in Phase E. Requires structural decision: do we expose Context to Lua at all in the sandboxed path, and if yes, with what reduced surface? Suggest deferring until Phase F UI work pins down which Context calls user scripts legitimately need. — raised by Team E
Q-E-6: full DSPScript::validate render-side dry run remains gated behind #if 0. The original block invoked node_render against synthetic AudioBuffer + MidiPipe at validation time, which is structurally fraught when the host hasn't allocated audio resources yet. Re-enabling needs a separate refactor of the validation harness. Not a security regression — the lightweight ScriptLoader compile-check now in place catches malformed scripts. — raised by Team E
[00:50:32] team-c2: Part A landed (f653b167) — engine block hoisted above empty-graph early-return; element_app builds; PROJECT OVERVIEW snapshot now carries deviceName/sampleRate/bufferSize/latency on empty graphs
[00:51:55] team-c2: Part B landed (04049054) — InspectorHub PROJECT OVERVIEW subscribes to useEngineSnapshotStore; vitest 41/41; main chunk 209.85 kB; TOTAL CPU / HOST CPU now both surface engine snapshot CPU (graph cpuLoad placeholder removed)
team-snap-schema: Part 1 landed 9ba350bc — per-block cpuLoad + latencyMs now flow through buildActiveGraphJson, mapBlock reads them. cpuLoad=0 (FIXME) until per-Processor CPU source exists.
team-snap-schema: Part 2 landed 68c19681 — transportTimecode + transportFrame in engine snapshot, useEngineSnapshotStore owns timecode write-through, sampleRateLabel typo dropped.
