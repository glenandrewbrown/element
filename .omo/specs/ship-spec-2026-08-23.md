# Spec: Ship a Finished Element (post-consolidation) — deep-dive crystallization

Date: 2026-08-23. Author: deep-dive (trace + interview, Glen-answered). Ambiguity: ~0.15 (≤0.2 gate).
Trace evidence: `.omc/specs/deep-dive-trace-evaluate-the-post-repo-merge.md`.
Branch: `wave-3-leanfast-ux` @ `f8861b5d`.

## Goal

Ship a **finished Element**: standalone app AND Element plugin builds (AU + VST3) as first-class, verified deliverables, packaged as an **ad-hoc signed local installer** (notarization deferred). "Finished" = full internal quality bar: complete 37-verdict UI audit closed (Edit-mode scope) + full test-debt payoff (all 28 disabled suites re-enabled and green) + zero failing tests.

## Acceptance Criteria (ship gates)

1. **Repo consolidation closed out:**
   - Rewritten (purged) history force-pushed to `origin` (github.com/glenandrewbrown/element); origin branches replaced. Until pushed: NO non-dry-run fetch/pull from origin (would re-import purged blobs).
   - `git gc --prune=now` run; single pack; `.git` size sane; second-pack (242M, 21:44) provenance understood before prune.
   - `gui-vs-features` (11 commits, PreferencesModal/WebView parity) **ported** (merge/rebase + conflict triage vs current UI).
   - `archive/pizmidi-native-nodes` (9 commits, InlineParamControl MIDI-FX) **ported**.
   - `element-2-dirty-2026-08-15.patch`, `element-A-webview-dirty-2026-08-15.patch`, `stash@{0}` **explicitly dropped** (Glen decision 2026-08-23) — record in a consolidation-closeout note, then drop stash.
2. **Tests:**
   - `SandboxRealProcessTests/RealProcessRoundTrip` fixed (currently reliable failure: 5-7 of 8 required cycles; test/engine/SandboxRealProcessTest.cpp:141). Root-cause first — merge regression vs environment (worker binary signing/spawn) vs real IPC defect.
   - All 28 disabled suites re-enabled and green: SessionChangedTest, BridgeContractTests, SessionServiceObserver/FileOps, RingBuffer, RerouteNode, NodeEdgeCase, EQFilter, Compressor, MidiRouterNode, MidiSetList, MidiEngine, AudioFilePlayer, GraphManager (+Connection/Change), AudioMixerProcessor, PlaceholderProcessor, TransportMonitor, GraphNodeMidiChannel, MidiChannelFilterNode, MidiChannelSplitter, PortBuffer, NodeFactoryExtended, MidiVelocityAmp, GraphConnectionValidation, GraphTopologyRobustness, SandboxCVTransport.
   - Full `ctest` 100% pass on fresh `build-merged`; webview `tsc -b` + `npm run build` + vitest green.
3. **UI — 37-verdict audit (Edit-mode scope):**
   - Audit `.omo/bakeoff/VERDICTS.md` against actual `webview/src/components/` implementation; produce per-verdict status (done / partial / missing / N-A-shelved).
   - Close every gap for Edit-mode verdicts. **D3 stands**: Perform mode, Scenes, DashboardBuilder, MacroDashboard stay shelved and unwired — their verdicts marked N/A-shelved, not built.
   - VERDICTS.md (or successor tracker) updated with real completion state.
4. **Plugin deliverables:**
   - Build with `ELEMENT_BUILD_PLUGINS=ON`.
   - **AU verified in Logic Pro**: load, edit UI, save/restore state, close, remove — no crash (explicit CF1-regression check: AX teardown with VoiceOver on).
   - **VST3 verified in a VST3 host** (Live/Reaper): same lifecycle checks.
5. **Package:** `scripts/sign-all-macos.sh` (ad-hoc) + `installer/build_pkg.sh` → versioned PKG+DMG containing standalone + plugins + sandbox helper. Installs and runs on a clean account/machine.
6. **Docs re-baselined:** CLAUDE.md + `.omo/PROJECT-STATE.md` corrected (CF1 fixed `8c37eafd`; sandbox mode=1 shipped `36e87345`; bake-off method superseded by QA-wave method; doc index pruned). Template-contaminated root `PRODUCT.md`/`DESIGN.md` ("Platform: web / React 19 / Vite 8") corrected or removed.

## Constraints

- Realtime-safety rules per CLAUDE.md (no allocs/locks on audio thread; atomic pointer swaps).
- macOS deployment target 14.0; build dir `build-merged` (dev), `build-release` for installer.
- CMake GLOB_RECURSE: reconfigure after new .cpp files.
- Origin fetch/pull embargo until force-push done.
- Terminology (Project/Board/Block/Cable…) mandatory; terminology-guard test enforces.
- Method for UI gap-closing: the working QA-wave/live-feedback method — NOT the dead Stitch bake-off pipeline; VERDICTS content remains the build spec.

## Non-Goals

- Notarization / Developer ID distribution (deferred; ad-hoc signing only).
- Public GitHub release.
- Perform mode, Scenes, Dashboard Builder, MacroDashboard (D3 shelving stands).
- CLAP/AUv3 verification (build may exist; not a ship gate).
- Commercialization/licensing/rebrand decisions (research exists 06-10; no decision — out of scope).
- Porting the 2026-08-15 dirty patches or stash@{0} (explicitly dropped).

## Assumptions Exposed (interview-confirmed)

- 2.5-month gap (06-12→08-23) was a pause; no authoritative off-repo work. Studio-checkout PRODUCT.md/DESIGN.md = template noise.
- No CI/collaborators depend on origin's old SHAs (force-push safe).
- Current wave-3 UI is the ship UI baseline; audit determines the delta, not a redesign.

## Technical Context (from trace, evidence-backed)

- Build fully green today: C++ exit 0 (fresh configure 21:24), webview build+tsc clean, dist newer than src (CMakeLists.txt:122 embeds dist).
- ctest 155/156 enabled-pass; single reliable failure = SandboxRealProcessTests.
- CF1 (AU-in-Logic AX use-after-free) FIXED `8c37eafd` 2026-06-02, re-verified 06-09. The 06-12 Kontakt-window UAF fixes (`6bfe361b`) are a distinct family.
- Sandbox out-of-process hosting: default `pluginSandboxMode=1` shipped `36e87345` 06-10; helper packaged in installer.
- ~80-90% of 37 verdicts exist functionally in `webview/src/components/`; VERDICTS.md frozen untracked.
- Purge verified clean on all local refs; `pre-filter-head` 71266440 == live `9040a04f` (filter-repo commit-map) — nothing lost.
- History: 147 commits 06-01→06-12; 0 until 08-23; 6 consolidation commits today.

## Trace Findings (summary)

- Lane 1 (build health): mostly REFUTED — merge did not break the build; residue = 1 reliably-failing sandbox test + 28 disabled suites.
- Lane 2 (consolidation): PARTIAL — purge clean, but push/gc/port-decisions pending; unported work inventoried; nothing lost.
- Lane 3 (spec staleness): CONFIRMED — canonical docs 12 weeks stale, falsified on CF1 and sandbox status; stale premise already caused a wasted parallel re-implementation (studio checkout).
- Converged mechanism: no single current source of truth (code/history/plan) — this spec + re-baselined docs fix that.

## Interview Transcript (condensed)

1. Ship bar? → **Standalone + plugin builds** (AU/VST3 first-class, tested).
2. Unported work? → Port **gui-vs-features** + **pizmidi-native-nodes**; drop dirty patches + stash.
3. Origin? → **Force-push rewritten history**.
4. 2.5-month gap? → Pause; resume from repo; PRODUCT.md/DESIGN.md noise.
5. Plugin matrix? → **AU in Logic + VST3 in a VST3 host**.
6. UI threshold? → **Full 37-verdict audit first**, close gaps before ship.
7. Test debt? → **Re-enable + fix all 28** before ship.
8. Release form? → **Ad-hoc signed local build**.
9. Perform mode? → **D3 stands — Edit-mode verdicts only**.
10. Sequencing? → **Planner decides**.
